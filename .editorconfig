# ============================================
# 🚀 AppliPlumeroWeb — .editorconfig
# Harmonisation du style de code entre éditeurs
# ============================================

# Indique à tous les éditeurs de respecter ces règles
root = true

# Règles générales applicables à tous les fichiers
[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Règles spécifiques pour Markdown (préserve les espaces)
[*.md]
trim_trailing_whitespace = false

# Règles spécifiques pour YAML / JSON
[*.{yml,yaml,json}]
indent_size = 2